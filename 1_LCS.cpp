#include<bits/stdc++.h>

int main()
{
    string str1,str2;   // Defining two strings

    cin>>str1;          // Taking input strings
    cin>>str2;

    len1=str1.size();   // Assigning the length of the strings to len1 and len2
    len2=str2.size();

    vector<vector <int>>  LCS[len1+1][len2+1];


}